Create resources using terraform 
connect to required machine using ssh 
Install all dependencies from install.sh (vim install.sh => ctrl+c ctrl+v => chmod +x install.sh)
Add credentials of 
  github to give access to repo 
  docker hub to push docker images (user name and token)
Create new jop 
  New Item 
    Name 
    PipeLine
  pipeline script from source code 
      repo url
        it will run jenkins file exists in repo when I build the pipeline
      credentials
      branchName 
build 
check logs and outputs 

